<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExplorationXMLTemplate"
        tools:targetApi="31">
        <activity
            android:name=".activity.fragment_with_view_model.FragmentWithViewModelActivity"
            android:exported="false" />
        <activity
            android:name=".activity.bottom_navigation.BottomNavigationActivity"
            android:exported="false"
            android:theme="@style/Theme.ExplorationXMLTemplate" />
        <activity
            android:name=".activity.basic.BasicActivity"
            android:exported="false"
            android:theme="@style/Theme.ExplorationXMLTemplate" />
        <activity
            android:name=".activity.empty.EmptyActivity"
            android:exported="false" />
    </application>

</manifest>